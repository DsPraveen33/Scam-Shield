## Packages
recharts | For visualizing risk scores and dashboard stats
framer-motion | Smooth transitions and animations
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes

## Notes
Authentication is handled by Replit Auth (useAuth hook).
API endpoints follow strict Zod schemas defined in @shared/routes.
Scan results include complex JSON details (reasoning, flags).
